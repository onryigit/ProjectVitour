@model List<ProjectVitour.Entities.Destination>
@{
    ViewData["Title"] = "Destinasyon Yönetimi";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="page-header d-flex justify-content-between align-items-center mb-4">
    <div>
        <h2 class="page-title text-navy fw-bold">Destinasyon Yönetimi</h2>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/AdminDashboard/Index" class="text-muted">Admin</a></li>
                <li class="breadcrumb-item active" aria-current="page">Destinasyonlar</li>
            </ol>
        </nav>
    </div>
    <a href="/AdminDestination/CreateDestination" class="btn btn-primary rounded-3 shadow-sm">
        <i class="bi bi-plus-lg me-2"></i> Yeni Destinasyon Ekle
    </a>
</div>

<div class="card border-0 shadow-sm rounded-4 overflow-hidden mb-5">
    <div class="card-header bg-white p-4 border-0">
        <h5 class="card-title mb-0 fw-bold"><i class="bi bi-geo-alt text-primary me-2"></i> Kayıtlı Bölgeler</h5>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="bg-light text-muted small text-uppercase">
                    <tr>
                        <th class="ps-4 py-3">Lokasyon Bilgisi</th>
                        <th class="py-3">Açıklama</th>
                        <th class="py-3">Durum</th>
                        <th class="text-end pe-4 py-3">İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    @if(Model != null)
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td class="ps-4 py-3">
                                    <div class="d-flex align-items-center gap-3">
                                        <div class="rounded-circle bg-light border d-flex align-items-center justify-content-center" style="width:45px; height:45px;">
                                            <i class="bi bi-image text-muted"></i>
                                        </div>
                                        <div>
                                            <div class="fw-bold text-dark mb-1">@item.CityName</div>
                                            <small class="text-muted">@item.CountryName</small>
                                        </div>
                                    </div>
                                </td>
                                <td class="py-3 text-muted"><small>@(item.Description?.Length > 40 ? item.Description.Substring(0,40)+"..." : item.Description)</small></td>
                                <td class="py-3">
                                    @if(item.Status) {
                                        <a href="/AdminDestination/ToggleStatus/@item.DestinationID" class="badge bg-success-subtle text-success p-2 rounded-pill text-decoration-none">Aktif <i class="bi bi-toggle-on ms-1"></i></a>
                                    } else {
                                        <a href="/AdminDestination/ToggleStatus/@item.DestinationID" class="badge bg-secondary-subtle text-secondary p-2 rounded-pill text-decoration-none">Pasif <i class="bi bi-toggle-off ms-1"></i></a>
                                    }
                                </td>
                                <td class="text-end pe-4 py-3">
                                    <div class="btn-group">
                                        <a href="#" class="btn btn-sm btn-light text-warning shadow-sm"><i class="bi bi-pencil-fill"></i></a>
                                        <a href="/AdminDestination/DeleteDestination/@item.DestinationID" class="btn btn-sm btn-light text-danger shadow-sm" onclick="return confirm('Emin misiniz?')"><i class="bi bi-trash-fill"></i></a>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
        @if (Model == null || !Model.Any())
        {
            <div class="text-center py-5">
                <i class="bi bi-geo-alt text-muted opacity-50" style="font-size: 4rem;"></i>
                <h6 class="mt-3 text-muted fw-bold">Kayıtlı Destinasyon Yok</h6>
            </div>
        }
    </div>
</div>